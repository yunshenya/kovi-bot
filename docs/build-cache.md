# 构建缓存

开发和 CI 默认关闭 Cargo 增量编译，并将调试信息限制为行号表。这样可以保留基本回溯信息，
同时避免长期运行的开发机不断积累增量对象。

查看占用：

```sh
du -sh target
```

清理本地生成物：

```sh
./scripts/clean-target.sh
```

`target/` 是可重建的未跟踪目录，不应纳入发布包、备份或生产服务器。生产部署只上传
release 二进制和配置，不上传本地 Cargo 缓存。
